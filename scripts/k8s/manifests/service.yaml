apiVersion: v1
kind: Service
metadata:
  name: merclaw
  labels:
    app: merclaw
spec:
  type: ClusterIP
  selector:
    app: merclaw
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
