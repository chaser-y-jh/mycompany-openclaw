apiVersion: v1
kind: ConfigMap
metadata:
  name: merclaw-config
  labels:
    app: merclaw
data:
  merclaw.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.merclaw/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "MerClaw Assistant",
            "workspace": "~/.merclaw/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # MerClaw Assistant

    You are a helpful AI assistant running in Kubernetes.
